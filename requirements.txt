Django>=5.2.6
# mysqlclient вынесен в requirements-mysql.txt — нужен системный MySQL dev SDK при pip install
# (на Vercel/serverless сборка падает). Для SQLite (по умолчанию без DB_NAME в .env) достаточно этого файла.
# PostgreSQL (DATABASE_URL / Neon / Supabase / Vercel Postgres) — бинарные wheels, без pkg-config
dj-database-url>=2.2.0
psycopg[binary]>=3.2.0
python-dotenv>=1.1.1
django-browser-reload>=1.21.0
django-watchfiles>=1.4.0
pillow>=11.3.0
djangorestframework>=3.15.0
django-cors-headers>=4.6.0
celery[redis]>=5.4.0
redis>=5.0.0
httpx>=0.28.0
tenacity>=9.0.0
reportlab>=4.0.0
gunicorn>=23.0.0
whitenoise>=6.8.0
pytest>=8.0.0
pytest-django>=4.8.0
